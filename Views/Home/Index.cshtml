@model HomeViewModel

<partial name="_Carousel" />

<h2>Os preferidos da semana</h2>
<h4>Que tal uma roupa nova?</h4>

<div class="row">
    @foreach(var roupa in Model.RoupasPreferidas)
    {
        <partial name="_RoupaResumo" for="@roupa"/>

        <br />
        <br />
    }
</div>